#include <bits/stdc++.h>
#include <cstdio> // Add this line

using namespace std;

int main () {
    double x, y;
    cin >> x >> y;
    double z;
    z = pow(x, y);
    cout << fixed << setprecision(4) << z << endl;
}