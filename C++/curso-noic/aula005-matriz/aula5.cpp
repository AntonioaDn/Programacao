#include <bits/stdc++.h>

using namespace std;

int main () {
    /*
    faca um programa para ler os elementos de uma matriz 3x3 e imprimir a soma de cada 
    coluna da matriz.
    */
   	/*
   	int matriz[3][3];
	for (int i=0; i < 3; i++) {
		for (int j=0; j < 3; j++) {
			// Le todos os elementos da matriz.
			cin >> matriz[i][j];
		}
	}
	int colunas[3] = {0, 0, 0}, v;
	for (int i=0; i < 3; i++) {
		for (int j=0; j < 3; j++) {
			colunas[i] += matriz[j][i];
		}
	}
	cout << "Coluna: " << colunas[0] << endl;
	cout << "Coluna: " << colunas[1] << endl;
	cout << "Coluna: " << colunas[2] << endl; 
	*/

}