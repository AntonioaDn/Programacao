#include <bits/stdc++.h>

using namespace std;

int main () {
    /*
    Basicamente em C++ o if eh igual ao C. Podendo aninhar else if e else a ele.
    Operadores de comparacao tambem sao iguais.
    && = AND; || = OR; ! = NOT
    */
}