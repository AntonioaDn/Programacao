#include <bits/stdc++.h> 

using namespace std;

int main () {
    bool a, b;
    cin >> a >> b;
    if (!a)
        cout << 'C' << endl;
    else {
        if (b)
            cout << 'A' << endl;
        else 
            cout << 'B' << endl;
    }
}