#include <stdio.h>
int main () {
    int N, F1, F2, MDC, c, aux;
//Começar recebendo o N para Repetir o loop quantas vezes o usuário quiser
    scanf("%d", &N);
    for (int i=1; i <= N; i++) {
        scanf("%d %d", &F1, &F2);
        c = F1%F2;
        while (c!=0) {
            aux =
        }
    }
    return 0;
}
